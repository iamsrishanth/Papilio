     1→395 /home/z/my-project/worklog.md
     2→1. Lighthouse CI (mobile perf ≥ 85) — still needs a production build; forbidden in this sandbox.
     3→2. Real-device QA of the 3D hero on low-end Android remains open.
     4→3. Pre-production client tasks unchanged: production domain in SITE_URL, real menu import, real photography swap (the new OG/icon art is AI-generated placeholder-grade).
     5→4. Ideas for round 5: a tiny "share this page" on /visit (ShareButton is reusable); gallery photo captions could gain copy-to-clipboard for IG reposts; consider OG twitter:card per-route check; menu rail could get aria-expanded on mobile; README/handover for the client (how to swap SITE_URL, images, menu data).
     6→5. Dev server reaping happened mid-round — always re-check port 3000 first; restart: kill zombie next-server if 99% CPU, rm -rf .next, `setsid -f bun run dev > dev.log 2>&1 < /dev/null`.
     7→
     8→---
     9→Task ID: 15 (webDevReview round 5)
    10→Agent: Z.ai Code (main)
    11→Task: Baseline QA, then feature/polish round: share-this-page on /visit, copy-phone icon button, founder Instagram links, gallery ?tag= deep links, lightbox copy-caption, header scrolled elevation, ::selection palette refinement, DishCard hover lift.
    12→
    13→Work Log:
    14→- Baseline QA (fresh agent-browser session): all 6 routes + 404 + sitemap + robots + manifest return 200, zero console/page errors, canvas 1→0 lifecycle intact, no 390px overflow anywhere. Stable → feature/polish tier.
    15→- NEW FEATURE — Share this page on /visit: reusable ShareButton (native share → clipboard fallback + sonner toast) placed as a quiet tertiary link under the address card CTA row (hairline divider, -ml-4 icon alignment). Verified with stubbed clipboard: copies origin+/visit.
    16→- NEW FEATURE — copy phone number: CopyButton gained an iconOnly prop (44px square pill, aria-label swap "Phone number copied — Copy phone number", no visible text) rendered beside the phone row in the /visit Contact card (li is now a flex row). Verified: copies "+91 90003 16366" exactly.
    17→- NEW FEATURE — founder Instagram links: StoryFounder type + story.ts entries now carry instagramHandle/instagramUrl (@Radhasuvidha, @ChefSiddharthaReddy — from the verified research corpus / café IG bio; clearly commented in the module). Founder cards render an IG link (min-h-11, icon + handle, aria-label "«name» on Instagram — @handle"), cards gained hover shadow-lift + icon micro-tilt. Verified in DOM: 3 instagram.com links on /story (2 founders + footer café handle).
    18→- NEW FEATURE — gallery tag deep links: GalleryGrid reads /gallery?tag=room|coffee|patisserie|oven (useSearchParams; unknown values reset to All) and syncs the chosen tag back via history.replaceState (no history entries) — mirrors the menu filter deep-link pattern exactly. gallery/page.tsx wraps GalleryGrid in <Suspense> with a masonry-shaped skeleton fallback (8 aspect-ratio placeholders, no layout shift). sr-only live note announces the active filter. Verified: ?tag=patisserie → 5 of 11 + chip active; All click clears the URL; Coffee click → ?tag=coffee (2 photos); ?tag=bogus ignored → All (11 figures).
    19→- NEW FEATURE — lightbox copy caption: quiet 44px pill under the nav row in PhotoLightbox (Copy icon, "Copy caption" → "Copied" swap, aria-label "Caption copied", clipboard API + textarea fallback, state resets on photo change via useEffect on activeIndex). Verified: copies the exact caption text; label resets after navigating; arrow-key nav + ESC close unaffected (1/11 → 2/11 → 1/11 → closed).
    20→- STYLING — header scrolled elevation: rAF-throttled passive scroll listener (scrollY > 8) toggles a firmer hairline (border-linen), bg-cream/95 and warm shadow-card; transition is colors/shadow only, height constant so no layout shift; motion-reduce:transition-none. Verified live: shadow appears at 600px, disappears at top (first test's false negative was smooth-scroll still animating — instant-scroll retest confirms).
    21→- STYLING — ::selection refinement: light surfaces get a caramel 22% tint (espresso text); espresso/dark surfaces ([class~=bg-surface-espresso]/[class~=bg-espresso], self + descendants) get a butter 32% tint with cream text. Verified present in the served CSS chunk (rules sit inside @layer base, so a naive top-level CSSOM scan misses them — grep the served file instead).
    22→- STYLING — DishCard hover lift: cards now lift -translate-y-0.5 + shadow-lift on hover (transition-[box-shadow,transform], motion-reduce:transform-none) on top of the existing image zoom.
    23→- Fixed during round: an initial TDZ bug in PhotoLightbox (effect referenced activeIndexNext before declaration) — restructured to derive activeIndex first; CopyButton aria/label logic cleaned (aria-live stays, label swap unified).
    24→- QA battery all green: lint exit 0; all routes 200; fresh-session console clean; typo + placeholder quarantine zero hits on all rendered pages (incl. Binge/confetrio guards); founder handles render on /story; menu regressions pass (Veg → 173 of 187, Signature+Veg → 1 of 187); reduced-motion: 0 canvases, 0 hidden reveals, gallery deep link works under RM; 390px: no overflow on /visit /story /gallery; VLM review of the new visit share row + founder cards: "No defects."
    25→
    26→Stage Summary:
    27→- Round-5 shipped: share-this-page on /visit, icon-only copy-phone, founder Instagram links (verified handles), deep-linkable + shareable gallery subject filters with Suspense fallback, lightbox copy-caption, header scrolled elevation, palette-pure ::selection (caramel light / butter dark), DishCard hover lift.
    28→- Contracts preserved: one filled primary CTA per view (all new controls are tertiary/quiet pills), butter only on espresso surfaces, no fabricated facts (founder handles from the verified research corpus, everything else interpolates content modules), reduced-motion honored, 44px touch targets everywhere, canvas dispose contract intact.
    29→
    30→Unresolved risks / next-phase recommendations:
    31→1. Lighthouse CI (mobile perf ≥ 85) — still needs a production build; forbidden in this sandbox.
    32→2. Real-device QA of the 3D hero on low-end Android remains open.
    33→3. Pre-production client tasks unchanged: production domain in SITE_URL, real menu import, real photography swap.
    34→4. Ideas for round 6: keyboard "g" shortcut to open gallery lightbox? FAQ deep-link chips on home visit band; per-route twitter:card spot audit (all verified summary_large_image so far); gallery lightbox could preload the next image; consider aria-expanded semantics for the mobile menu rail.
    35→5. Dev server reaping: re-check port 3000 each round; restart with `setsid -f bun run dev > dev.log 2>&1 < /dev/null` (kill -9 zombie next-server + rm -rf .next if wedged).
    36→
    37→---
    38→Task ID: 16 (webDevReview round 6)
    39→Agent: Z.ai Code (main)
    40→Task: Baseline QA, then feature/styling round: home Instagram strip ("Follow the café"), lightbox next-image preload, menu editorial section numbers, route-level loading skeletons, gallery expand affordance, animated link-underline utility.
    41→
    42→Work Log:
    43→- Baseline QA (fresh agent-browser session): all 6 routes + custom 404 200, zero console/page errors, canvas 1 on home / 0 elsewhere (lifecycle intact), no 390px overflow anywhere, "/" keyboard shortcut handler confirmed live. Project stable → feature/styling tier per round mandate.
    44→- NEW FEATURE — home "Follow the café" Instagram strip (src/app/page.tsx, between patisserie teaser and visit band): eyebrow + Playfair H2 + neutral copy ("our 1.4K Instagram community sees them first" — igCommunity from site.ts) + @papiliocafe.in text link (link-underline) + 5-photo square-crop strip (grid-cols-2 sm:3 lg:5; existing assets pulled from content/gallery.ts so alt text stays descriptive and nothing is claimed as a literal IG post; every tile links to the verified profile). No filled CTA added. VLM-verified desktop + mobile + section-rhythm screenshots: PASS (palette, alignment, whitespace).
    45→- NEW FEATURE — lightbox next-image preload (photo-lightbox.tsx): getImageProps (next/image) builds the exact optimized srcSet/sizes the live <Image> uses; a <link rel=preload as=image imagesrcset imagesizes> warms it on every photo change, removed on cleanup. NOTE (hard-won): the .imageSrcSet IDL property is NOT honored for dynamically created links in Chromium — setAttribute('imagesrcset'/'imagesizes') is required (verified via performance.getEntriesByType: initiatorType "link"). A detached new Image() with srcSet also never resolves candidates in headless Chromium — don't use that path.
    46→- NEW FEATURE — menu editorial section numbers (menu-explorer.tsx): stable 01–21 indices derived from the ORIGINAL menu order (render under any filter combination — verified ?diet=veg keeps 01/02/03… with 20 visible sections). tnum +0.14em tracking, caramel/80 on light, butter/70 on the espresso coffee band (VLM: "legible, elegant, not garish"), aria-hidden, prints espresso via the existing print rule (verified in the r6-menu-print.pdf: "02 Wraps", "05 Burgers", "20 Counter Desserts").
    47→- NEW FEATURE — route-level loading skeletons: src/app/menu/loading.tsx + src/app/gallery/loading.tsx (server components, shape-matched to their pages: header block, search/chips row, section cards / masonry placeholders, animate-pulse). Skeletons are aria-hidden; reduced-motion renders them static via the global RM rule.
    48→- NEW FEATURE — gallery expand affordance (gallery-grid.tsx): 36px cream/90 backdrop-blur pill with Maximize2 icon, bottom-right inside the photo button, opacity-0 → group-hover:opacity-100, pointer-events-none, motion-reduce safe. VLM (forced-visible): size, centering, no corner clipping, contrast — all PASS.
    49→- STYLING — animated link-underline utility (globals.css @utility link-underline): 1.5px caramel rule painted as a background-image (3px bottom padding on the inline box — no layout shift, no descender collision), grows from the left on hover/focus-visible in 300ms; global RM rule collapses the transition. Applied to plain text links only (pill links keep text-decoration): home ×3 (See the full menu / Filter the menu / FAQ link) + feed handle link, header "Also on Zomato", visit phone link, story founder IG links, patisserie "See every dessert", 404 "Find the café". Verified compiled in served CSS (.link-underline + :hover/:focus-visible both present) and painted (computed bg-image/size/padding).
    50→- QA battery all green: bun run lint exit 0; all routes 200 with zero errors in a fresh session; menu deep links (?diet=veg → 173 of 187, ?signature=1 → 4 of 187, #coffee → settles at exactly 112px with aria-current chip); gallery ?tag=patisserie → 5 of 11; reduced-motion: 0 canvases, 0 hidden reveals, numbers render, no overflow; 390px: scrollWidth exactly 390 on all 6 routes; typo/placeholder quarantine zero hits on all rendered pages; print masthead + numbers verified via pdftotext; dev.log clean.
    51→- QA-environment notes for future rounds: (1) Tailwind v4 wraps hover/group-hover variants in @media (hover: hover) — headless Chromium reports (hover: none), so ALL hover styling is inert in agent-browser; verify the rule exists in served CSS + force-check computed styles instead of trusting visual hover. (2) agent-browser network request log misses some programmatic fetches; performance.getEntriesByType('resource') is the reliable witness (initiatorType shows "link" for preloads).
    52→
    53→Stage Summary:
    54→- Round-6 shipped: home Instagram social-proof strip (verified handle/community stat, zero fabricated facts, no new filled CTA), lightbox next-image preloading via link-rel-preload with exact optimized URLs, stable editorial menu numbering 01–21 (light + dark + print), route-level brand skeletons for /menu and /gallery, gallery photo expand affordance, and an animated caramel link-underline utility applied across 7 files.
    55→- Contracts preserved: one filled primary CTA per view (all new elements are text/tertiary), butter-gold only on espresso surfaces, palette-pure, reduced-motion honored everywhere, canvas dispose contract intact, print path still correct.
    56→- Regression status: all round-1–5 features re-verified green (search, filters, deep links, hash anchors with settle re-align, lightbox nav/copy/swipe, back-to-top, open-now badge, reveals, drop caps, owner quote, FAQ JSON-LD, PWA manifest, breadcrumbs, print masthead).
    57→
    58→Unresolved risks / next-phase recommendations:
    59→1. Lighthouse CI (mobile perf ≥ 85 with Scene A) — still needs a production build; forbidden in this sandbox.
    60→2. Real-device QA of the 3D hero on low-end Android remains open.
    61→3. Pre-production client tasks unchanged: production domain in SITE_URL, real menu import, real photography swap (the IG strip is representative placeholder photography, honestly worded).
    62→4. Ideas for round 7: feed strip could get a subtle hover scrim with the caption; menu rail could mirror the section numbers (currently header-only); gallery lightbox could preload PREV as well for bidirectional swipe; consider JSON-LD ImageObject list on /gallery; "g" keyboard shortcut to open the gallery lightbox remains unimplemented.
    63→5. Dev server reaping: re-check port 3000 each round; restart with `setsid -f bun run dev > dev.log 2>&1 < /dev/null` (kill -9 zombie next-server + rm -rf .next if wedged).
    64→
    65→---
    66→Task ID: 17 (webDevReview round 7)
    67→Agent: Z.ai Code (main)
    68→Task: Baseline QA, then feature/styling round: gallery ImageObject JSON-LD, bidirectional lightbox preload, feed-strip follow tile + caption scrims, story reading-progress bar, numbered desktop menu rail.
    69→
    70→Work Log:
    71→- Baseline QA (fresh agent-browser session; NOTE: first eval after `close --all` + `open` can race the browser launch and read about:blank — re-run the eval after ~2s if url is "blank"): all 6 routes 200, zero console/page errors, canvas 1 home / 0 elsewhere, round-6 features intact (5 feed tiles → later 4 + follow, menu numbers, deep links ?diet=veg → 173 of 187, lightbox ArrowRight nav). Project stable → feature/styling tier.
    72→- NEW FEATURE — gallery ImageObject JSON-LD: galleryImagesLd() in lib/seo.ts (ItemList of 11 ImageObjects: contentUrl SITE_URL+src, name=caption, description=alt, width/height; deliberately no creator/license — placeholder photography). Wired into gallery/page.tsx via the JsonLd component. Verified in-DOM: 3 LD scripts (CafeOrCoffeeShop + BreadcrumbList + ItemList), 11 items, correct contentUrl.
    73→- NEW FEATURE — bidirectional lightbox preload (photo-lightbox.tsx): the preload effect now warms BOTH neighbours (index+1 and index-1, wrap-around), each with its own <link rel=preload as=image imagesrcset/imagesizes>, all links removed on cleanup. Verified from photo 1: next=coffee-pour AND prev=patisserie-gifting both fetched (performance.getEntriesByType initiatorType "link"); ArrowLeft navigation confirmed working (5 → 4 of 11).
    74→- NEW FEATURE — feed-strip follow tile: the 5th strip tile is now a deep-espresso (bg-espresso #2A1B10 — chip-rating grammar, matches the hero scroll cue) square card: butter Instagram glyph in a cream/10 circular chip (group-hover micro-tilt), Playfair "Follow" in cream, label-caps @papiliocafe.in in butter/80, hover lift + shadow-lift, full aria-label. VLM round 1 flagged the first attempt (bg-surface-espresso read "muddy/taupe" next to vivid photos + optical centering) → deepened to bg-espresso + pb-6 optical nudge → VLM round 2: PASS ("anchoring weight, premium, conversion point"). Now 4 photo tiles + 1 follow tile (mobile 2-col: 2 rows of photos + follow tile; sm 3-col: 3+2; lg 5-col: single row).
    75→- STYLING — feed photo caption scrims: each of the 4 photo tiles gains an espresso gradient scrim (from-espresso/80 via /35 to transparent) with the photo's caption in cream 2-line-clamped, opacity-0 → group-hover:opacity-100, pointer-events-none, aria-hidden (the link's aria-label already carries the caption for AT). Verified: 4 scrims in DOM, gradient painted, group-hover:opacity-100 rule present in served CSS (visual hover gated by the known headless (hover:none) limitation).
    76→- NEW FEATURE — reading progress bar (src/components/site/reading-progress.tsx): hairline 3px caramel bar fixed to the top of the viewport on /story (long editorial scroll incl. the 200vh wing-unfold zone). scaleX from origin-left (compositor-only), rAF-throttled passive scroll+resize listeners, cleanup on unmount, aria-hidden + no-print. Scroll-linked (user-driven, not animation) so it stays live under reduced-motion by design. Verified: scaleX(0) at top → 0.46 at 2000px; VLM: PASS (thin, warm caramel, no header overlap).
    77→- STYLING — numbered desktop menu rail: each desktop rail row now mirrors the section-header editorial number (01–21, tnum, +0.14em tracking, cocoa/45 → caramel/90 when active, hidden on the mobile chip rail). Numbers derive from the ORIGINAL menu order — stable under all filter combinations. VLM: PASS ("subtle, aligned, active row distinct").
    78→- QA battery all green: bun run lint exit 0; all 6 routes 200, zero errors in a fresh session; lightbox prev+next nav + copy-caption + ESC unaffected; #coffee anchor still settles at exactly 112px; reduced-motion: 0 canvases, story progress still tracks (by design), no overflow; 390px exactly on all 6 routes; typo/placeholder quarantine zero hits on all rendered pages; dev.log clean.
    79→- Round-6 regressions specifically re-verified: feed tiles render (now 4+follow), menu numbers 01–21 under ?diet=veg, rail numbers active-state color, link-underline utility unaffected, loading skeletons untouched.
    80→
    81→Stage Summary:
    82→- Round-7 shipped: ImageObject structured data for all 11 gallery photos, bidirectional lightbox preloading, a deep-espresso "Follow" tile closing the Instagram strip (VLM-verified twice), hover caption scrims on feed photos, a caramel reading-progress hairline on /story, and numbered-TOC desktop menu rail.
    83→- Contracts preserved: one filled primary CTA per view (follow tile is a dark card-link, not a caramel pill; zero new filled CTAs), butter only on espresso surfaces, palette-pure (follow tile deepened WITHIN the approved palette), zero fabricated facts (all values from content modules), reduced-motion honored, print unaffected, 44px targets.
    84→- VLM catch of the round: initial surface-espresso follow tile read weak next to photography — switched to bg-espresso + optical nudge; a good reminder that token-legal ≠ visually optimal for small dark tiles beside saturated photos.
    85→
    86→Unresolved risks / next-phase recommendations:
    87→1. Lighthouse CI (mobile perf ≥ 85 with Scene A) — still needs a production build; forbidden in this sandbox.
    88→2. Real-device QA of the 3D hero on low-end Android remains open.
    89→3. Pre-production client tasks unchanged: production domain in SITE_URL, real menu import, real photography swap.
    90→4. Ideas for round 8: ReadingProgress could also mount on /menu (rail gives wayfinding but the page is very long); follow tile could get a hover arrow or the caption-scrim treatment on mobile tap states; gallery lightbox could show the subject tags; consider a template.tsx route-fade transition (risk: double-animation with the menu/gallery loading skeletons — test carefully); "g" keyboard shortcut for the gallery remains unimplemented.
    91→5. Dev server reaping: re-check port 3000 each round; restart with `setsid -f bun run dev > dev.log 2>&1 < /dev/null` (kill -9 zombie next-server + rm -rf .next if wedged).
    92→
    93→---
    94→Task ID: 18 (webDevReview round 8)
    95→Agent: Z.ai Code (main)
    96→Task: Baseline QA, then feature/styling round: reading-progress on /menu, "G" keyboard shortcut for the gallery lightbox, ?photo deep links + copy-link, lightbox subject tags, follow-tile hover arrow, kbd-chip utility, text-wrap typography refinements.
    97→
    98→Work Log:
    99→- Dev server had been REAPED again (curl 000, port 3000 not listening) — restarted per the documented procedure: rm -rf .next + `setsid -f bun run dev > dev.log 2>&1 < /dev/null`; all routes re-verified 200.
   100→- Baseline QA (fresh agent-browser session): all 6 routes 200, zero console/page errors, canvas 1 home / 0 elsewhere, 390px scrollWidth exactly 390 everywhere, lint exit 0. Stable → feature/styling tier.
   101→- NEW FEATURE — ReadingProgress on /menu: the caramel hairline (src/components/site/reading-progress.tsx, previously /story-only) now also mounts on the menu page — 187 items over 21 sections is the longest scroll on the site. Verified: scaleX(0) at top → 0.136 at 2000px; no-print class present.
   102→- NEW FEATURE — "G" keyboard shortcut (gallery-grid.tsx): keydown listener opens the lightbox at the first photo of the current filter; skips modifier combos and text-entry contexts (input/textarea/select/contenteditable via document.activeElement), no-op while the dialog is open. Visible kbd hint ("Press [G] to open the lightbox") beside the filter chips, hidden on <sm. Suggested since round 5.
   103→- NEW FEATURE — ?photo= deep links + Copy link: GalleryGrid seeds BOTH ?tag= and ?photo=N (indexes relative to the filtered set; present-but-invalid values are corrected, e.g. ?tag=bogus → All + URL cleaned). The URL sync effect writes ?photo while a photo is open (composes: /gallery?tag=patisserie&photo=2) and strips it on close. PhotoLightbox gained a "Copy link" pill next to "Copy caption" (same inline Copied swap, shared writeClipboard helper with textarea fallback) — copies an ABSOLUTE URL.
   104→- BUG FOUND & FIXED DURING THE ROUND (the round's real catch): after rewriting the seed effect, ?tag=… deep links STOPPED applying on fresh loads (tag reset to "all", "11 of 11"). Root cause: the URL-sync effect ran on mount with PRE-SEED state (all/null) and replaceState-STRIPPED the deep-link params before the router's searchParams settled; the seed effect then re-ran against the stripped URL and reset the tag. The old code survived only because `if (!t) return` made the tag sticky. Fix: (1) sticky seed semantics — ABSENT params never reset state, present-but-invalid still correct; (2) a `seeded` state gate so the sync effect never runs its pre-seed mount pass. Re-verified all four deep-link shapes: ?tag=coffee → 2 of 11 + URL kept; ?tag=patisserie&photo=2 → dialog 3/5 + both params kept; ?photo=2 → dialog 3/11; ?tag=bogus → ignored + cleaned. Chip clicks / G / ESC / arrow-nav all still sync the URL.
   105→- NEW FEATURE — lightbox subject tags: the open photo's tags render as small uppercase butter-on-espresso chips under the caption (butter is DESIGN-legal on the espresso dialog surface), labels shared with the filter chips via galleryTagLabels.
   106→- STYLING — kbd-chip utility (globals.css @utility): shared visual chip for the "/" (menu search) and "G" (gallery) hints; menu-explorer's kbd refactored onto it. HARD-WON NOTE: Tailwind v4 does NOT generate a custom utility named `kbd` (identical to the HTML element name) — the class silently compiles to nothing (preflight's `code, kbd, samp, pre` is unrelated); renamed to `kbd-chip` and it generates fine. Verify with computed styles (fontSize 12px / 1px border / cream bg), not just class presence.
   107→- STYLING — typography: base-layer `h1,h2,h3 { text-wrap: balance }` + `p { text-wrap: pretty }` (editorial line breaking; no-ops where unsupported — both verified in served CSS). `accent-color: #9a5b1f` on html for any future native form controls.
   108→- STYLING — follow-tile hover arrow (home feed strip): ArrowUpRight slides in at the tile's top-right on hover (butter on espresso, cream/10 chip, motion-reduce safe) — the outbound-link affordance suggested in round 7. DOM + rule verified (visual hover gated by the known headless (hover:none) limitation; VLM reviewed a forced-visible state).
   109→- PERF — lightbox LCP: next/image on the open photo now carries `priority={open}` (the open photo IS the page's main content — and the LCP for ?photo= deep links; the retained photo during the close animation is already cached). Fresh-session console warnings for dessert-plate/facade cleared after the fix.
   110→- QA battery all green: lint exit 0; all 6 routes 200 with zero console/page errors in a fresh session (after console --clear); canvas 1 home / 0 elsewhere; 390px exactly on all 6 routes + the deep-linked lightbox state; menu regressions (?diet=veg → 173 of 187, search "paneer" → 12 of 187, #coffee anchor settles at 112.19px); reduced-motion: 0 canvases, 0 hidden reveals, gallery deep link opens under RM; typo/placeholder quarantine zero hits on all rendered pages; mobile lightbox (tags + both buttons + swipe hint) fits 390px with no overflow; VLM review of the 4 new states: PASS ×4 (lightbox tags row, filter row + kbd hint, menu progress hairline, follow tile).
   111→
   112→Stage Summary:
   113→- Round-8 shipped: menu reading-progress wayfinding, "G" lightbox shortcut with visible kbd affordance, shareable/openable ?photo= deep links that compose with ?tag=, a "Copy link" action in the lightbox, subject-tag chips under the lightbox caption, the follow-tile hover arrow, a shared kbd-chip utility, balance/pretty typography, accent-color, and a lightbox LCP priority fix.
   114→- Contracts preserved: one filled primary CTA per view (all new controls are quiet/tertiary), butter-gold only on espresso surfaces (tags/arrow follow the rule), palette-pure, zero fabricated facts (all copy interpolates content modules), reduced-motion honored, print unaffected (progress bar no-print), 44px touch targets.
   115→- The round's engineering catch: the mount-order race between the URL-sync effect and the deep-link seed (fixed via sticky seed + seeded gate) — documented above so future param-sync work starts from the safe pattern.
   116→
   117→Unresolved risks / next-phase recommendations:
   118→1. Lighthouse CI (mobile perf ≥ 85 with Scene A) — still needs a production build; forbidden in this sandbox.
   119→2. Real-device QA of the 3D hero on low-end Android remains open.
   120→3. Pre-production client tasks unchanged: production domain in SITE_URL, real menu import, real photography swap.
   121→4. Ideas for round 9: the story reading-progress and menu progress could share one component instance mounted in layout (currently two imports of the same component — fine as-is); lightbox could add a sr-only hint that "Copy link" shares the exact photo; menu rail could get aria-expanded semantics; consider a template.tsx route-fade (still risky with the loading skeletons); the follow-tile arrow could also appear on the photo tiles' scrim.
   122→5. Dev server reaping: re-check port 3000 each round; restart with `setsid -f bun run dev > dev.log 2>&1 < /dev/null` (kill -9 zombie next-server + rm -rf .next if wedged).
   123→
   124→---
   125→Task ID: 19 (webDevReview round 9)
   126→Agent: Z.ai Code (main)
   127→Task: Baseline QA (all routes + interactions), then feature/styling round: Cake Inquiry Composer on /patisserie, lightbox filmstrip, Copy-link a11y hint, feed-tile scrim arrows + focus-visible reveals.
   128→
   129→Work Log:
   130→- Dev server alive (curl 200 home/menu); lint exit 0; dev.log all-200, no errors.
   131→- Baseline QA (fresh agent-browser session): all 6 routes full DOM, ZERO console errors, canvas 1 home / 0 elsewhere, 390px exactly on all 6; menu ?diet=veg → 173/187, search "paneer" → 12/187; gallery G shortcut → 1/11, ArrowRight → 2/11, ESC closes. Stable → feature tier. NOTE: a wedged browser session (about:blank, empty body) right after `open` + `console --clear` resolved by closing and re-opening the session; server HTML was always fine — if DOM looks empty, close + re-open before suspecting the app.
   132→- NEW FEATURE (flagship) — Cake Inquiry Composer (src/components/patisserie/cake-inquiry-form.tsx + section on /patisserie): occasion chips (Birthday/Anniversary/Celebration/Other — generic inquiry categories, NOT product claims), date-needed (min=today), roughly-serves (digits-only, ≤3), notes (≤500 w/ tnum counter); a live espresso "What opens in WhatsApp" preview panel showing the EXACT message; "Send inquiry on WhatsApp" = the page's single filled primary → wa.me deep link built by whatsappLink(message); "Clear fields" appears only when dirty. NOTHING is stored/sent from the site (pure deep-link composer — no backend, per brief). Verified live: chips+date+notes compose the preview AND the wa.me href (?text=...); date renders "Date needed: 20 Sept 2026" (en-IN); clear resets to base greeting; agent-browser `fill` on date inputs does NOT fire React onChange — use the native value setter + input/change events when scripting (user interaction is unaffected).
   133→- CTA contract kept: the espresso custom-cake band's old primary is now a secondary "Plan your inquiry" (ArrowDown, in-page anchor to #cake-inquiry w/ scroll-mt-24); gifting secondaries unchanged. ONE visible filled primary per view confirmed via text-white discriminator (skip-link doesn't count — focus-only).
   134→- NEW FEATURE — lightbox filmstrip (photo-lightbox.tsx): 11 thumbnail jump-buttons under the prev/next row, active thumb butter-bordered + aria-current, row overflow-x-auto; hidden below sm (mobile keeps swipe + arrows + "Swipe to browse"). Verified: thumb 5 click → counter 5/11 + activeThumb sync; strip display:none at 390px; deep link ?photo=3 → 4/11 with hint present.
   135→- A11Y — "Copy link" now has aria-describedby → sr-only hint "Copies a link that reopens this exact photo in the gallery."
   136→- A11Y/STYLING — home feed photo tiles: caption scrim + NEW outbound arrow chip (butter on espresso/60, same grammar as the follow tile) now reveal on group-hover AND group-focus-visible. The compiled rule `.group-focus-visible\:opacity-100:is(:where(.group):focus-visible *)` verified in the SERVED CSS (a document.styleSheets walk that only iterates top-level cssRules misses nested media rules — grep the fetched stylesheet instead). VLM-verified forced-visible state: scrim + arrow PASS.
   137→- STYLING/CONTRAST — composer "optional" labels cocoa/70 → cocoa/85, char counter cocoa/60 → cocoa/75 (blended-on-ivory AA headroom for 12px text).
   138→- BUG (dev-time, caught by lint): my JSX comment "Cake inquiry planner" was missing its closing brace (`{/* ... */` not `*/}`) — ESLint/tsc pointed at the NEXT section; raw brace-count 48 vs 47 found it in seconds. Also dropped MessageCircle from patisserie imports while it was still used. Both fixed; lint exit 0.
   139→- Final regression battery ALL GREEN: lint 0; 6 routes full DOM + 0 console errors (fresh session); canvas 1/0; 390px exact on 6 routes; ?diet=veg 173/187; G → dialog 1/11 w/ 11 thumbs; ESC works; reduced-motion 0 canvases; dev.log clean. VLM reviews: mobile composer PASS (contrast fix applied), lightbox filmstrip PASS, desktop composer PASS ("static preview" flag = false positive — it saw the empty initial state; live composition proven by interaction tests).
   140→
   141→Stage Summary:
   142→- Round-9 shipped: the Cake Inquiry Composer (message builder + live preview + wa.me deep link — the single highest-value interactive feature for the café's custom-cake business, zero backend), the gallery lightbox filmstrip, the Copy-link sr-only hint, feed-tile outbound arrows with hover+focus-visible reveals, and small contrast lifts in the composer.
   143→- Contracts preserved: one filled primary per view (band CTA demoted to secondary anchor), butter only on espresso surfaces, palette-pure, zero fabricated business facts (occasions are inquiry categories; no lead times/flavours/prices claimed; "nothing is sent from the site" copy is literally true), 44px touch targets, reduced-motion honored, print unaffected.
   144→- QA-tooling notes for future rounds: (1) scripted .focus() does NOT match :focus-visible — verify group-focus-visible via the served CSS; (2) agent-browser fill on <input type=date> skips React events; (3) empty-DOM reads can be a wedged browser session — close/reopen first; (4) find the unclosed JSX brace by counting raw { vs } per line.
   145→
   146→Unresolved risks / next-phase recommendations:
   147→1. Lighthouse CI (mobile perf ≥ 85 with Scene A) — needs a production build; forbidden in this sandbox.
   148→2. Real-device QA of the 3D hero + the composer's native date picker on low-end Android remains open.
   149→3. Pre-production client tasks unchanged: production domain in SITE_URL, real menu import, real photography swap.
   150→4. Ideas for round 10: the composer could prefill from a /patisserie?cake=NAME deep link on the DishCards; a "gifting inquiry" variant of the composer (same pattern, different greeting); filmstrip could scroll its active thumb into view when navigating by arrows (currently only matters if the row overflows); menu dish rows could get a per-dish "copy name" affordance (quiet, tertiary); consider OpenGraph image for /patisserie (og-patisserie.png referenced in metadata — confirm the file exists).
   151→5. Dev server reaping: re-check port 3000 each round; restart with `setsid -f bun run dev > dev.log 2>&1 < /dev/null` (kill -9 zombie next-server + rm -rf .next if wedged).